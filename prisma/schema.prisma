generator client {
  provider = "prisma-client-js"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}


// ========== (inventory) ITEM =========
model item {
	id	 String @id @default(uuid())
	name String @unique
}